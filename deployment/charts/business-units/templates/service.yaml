apiVersion: v1
kind: Service
metadata:
  name: {{ include "business-units.fullname" . }}
  labels:
    {{- include "business-units.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "business-units.selectorLabels" . | nindent 4 }}
