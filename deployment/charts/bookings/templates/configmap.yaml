apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bookings.fullname" . }}
  labels:
    {{- include "bookings.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
