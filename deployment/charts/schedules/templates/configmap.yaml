apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "schedules.fullname" . }}
  labels:
    {{- include "schedules.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
